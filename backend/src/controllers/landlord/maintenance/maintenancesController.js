// src/controllers/landlord/maintenance/maintenanancesController.js

import prisma from "../../../libs/prismaClient.js";

const maintenancesController = async (req, res) => {
    // get all the landlord maintenances but first you need to query all the property first 
}

export default maintenancesController;
